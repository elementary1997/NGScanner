<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Экспортируемые PDF собираются в cacheDir/shared. -->
    <cache-path name="shared" path="shared/" />
    <!-- Скачанный APK обновления — cacheDir/updates. -->
    <cache-path name="updates" path="updates/" />
</paths>
