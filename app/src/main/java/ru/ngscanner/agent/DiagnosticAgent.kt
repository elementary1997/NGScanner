package ru.ngscanner.agent

import ru.ngscanner.llm.LlmProvider

/**
 * Диагностический агентный цикл.
 *
 * Прогоняет запрос пользователя через выбранный [LlmProvider], исполняет
 * запрошенные моделью инструменты (чтение данных с адаптера через
 * [ru.ngscanner.obd.Elm327]) и возвращает результаты модели, пока та не
 * сформирует финальный ответ. Не зависит ни от конкретного провайдера,
 * ни от транспорта.
 */
class DiagnosticAgent(
    private val provider: LlmProvider,
    private val model: String,
) {
    // TODO(этап 3): цикл provider.send() -> исполнить ToolCall через исполнитель
    //   инструментов -> вернуть ToolResult в следующий send(), пока не Final.
    //   clear_dtcs — только после подтверждения пользователя (гейт в UI).

    companion object {
        const val SYSTEM_PROMPT: String =
            "Ты — ассистент по диагностике автомобиля. У тебя есть инструменты для чтения " +
                "данных через OBD-II адаптер ELM327. Диагностируй по симптомам: читай коды " +
                "неисправностей, freeze frame и живые параметры, объясняй вероятную причину " +
                "простым языком и предлагай конкретные шаги проверки. Инструмент clear_dtcs " +
                "вызывай только после явного подтверждения пользователя."
    }
}
